# 🚘 Car Price Prediction Service

Репозиторий содержит проект по предсказанию стоимости автомобилей с использованием методов машинного обучения (Linear Regression, Lasso, Ridge, ElasticNet).

Проект состоит из двух частей:
1. **Исследование и моделирование** (`HW1_Regression_with_inference.ipynb`): полный цикл от EDA до подбора гиперпараметров.
2. **Веб-сервис** (`app.py`): интерактивное приложение на Streamlit для использования обученной модели (`model.pkl`) конечным пользователем.

---

## 🛠️ Что было сделано

В ходе работы был пройден полный пайплайн ML-разработки:

1.  **Обработка данных (Data Cleaning):**
    *   Обработаны дубликаты.
    *   Очищены текстовые столбцы (`mileage`, `engine`, `max_power`) от единиц измерения.
    *   Пропуски заполнены медианными значениями (без утечки данных из теста).
    *   Удален признак `torque` (из-за сложности парсинга) и `name`.

2.  **Разведочный анализ (EDA):**
    *   Выявлены зависимости: наибольшее влияние на цену оказывают `max_power` и `year`.
    *   Обнаружен сильный правый перекос целевой переменной `selling_price`. Применено **логарифмирование** таргета для значительного улучшения качества линейных моделей.
    *   Проанализированы корреляции и мультиколлинеарность (связь `engine` <-> `max_power`).

3.  **Построение моделей:**
    *   Обучена базовая **Linear Regression** только на вещественных признаках.
    *   Применена **Lasso** регрессия для отбора признаков (занулила вес `km_driven` при определенных параметрах).
    *   Построена **ElasticNet** для баланса между L1 и L2 регуляризацией.
    *   Добавлены категориальные признаки через **OneHotEncoding**.
    *   Обучена **Ridge** регрессия с подбором `alpha` через `GridSearchCV`.

4.  **Разработка бизнес-метрики:**
    *   Реализована кастомная метрика: *доля прогнозов, отличающихся от реальной цены не более чем на 10%*.

---

## 📊 Результаты и Метрики

Ниже приведены результаты лучших итераций моделей (метрики на тестовой выборке):

| Модель | Признаки | R² (Test) | MSE (Log scale) | Бизнес-метрика |
| :--- | :--- | :--- | :--- | :--- |
| **Linear Regression** | Числовые | 0.859 | 0.101 | **29.4%** |
| **Lasso** | Числовые | 0.856 | 0.103 | 29.3% |
| **Ridge (Best)** | Числовые + Категориальные | **0.872** | **0.092** | 28.1% |

### 📈 Интерпретация результатов:

1.  **Наибольший прирост качества:**
    повышение качества дало **добавление категориальных признаков** (особенно `transmission` и `seller_type`) в сочетании с Ridge-регуляризацией. Это позволило модели лучше различать сегменты авто.

2.  **Что не удалось:**
    *   **Бизнес-метрика:** парадоксально, но самая простая модель (Linear Regression) показала лучший результат по бизнес-метрике (29.4%), несмотря на меньший R². Это связано с тем, что Ridge лучше минимизирует ошибки сильных выбросов (уменьшая MSE), но может давать небольшое смещение для рядовых случаев, выбивая их из строгого коридора 10%.
    *   **Lasso:** ожидалось, что L1-регуляризация отберет признаки, но на практике почти все признаки (кроме `km_driven`) оказались значимыми, и Lasso не дала прироста качества.

---

## 💻 Оценка разработанного сервиса (Streamlit)

Сервис доступен в файле `app.py`.

### 1. Удобство использования
*   **Плюсы:** Интерфейс интуитивно понятен. Есть два режима работы: для одиночного ввода (через форму) и пакетной обработки (загрузка CSV). В режиме одиночного ввода предусмотрена валидация вводимых данных.
*   **Фича:** Реализована автоматическая предобработка данных внутри приложения (пользователю не нужно самому чистить строки или заполнять пропуски).

### 2. Визуализация
*   **Удачно:**
    *   График **Feature Importance** (веса модели) наглядно показывает пользователю, почему машина стоит столько денег (например, видно огромный вклад года выпуска и мощности).
    *   EDA-блок позволяет быстро оценить, на каких данных училась модель.
*   **Менее удачно:**
    *   Графики статичны (используется `pyplot` и `seaborn`). Следовало использовать `plotly` для интерактивности (зум, наведение).

### 3. Ограничения и планы
*   **Проблема:** в текущей версии удален признак `name` (Бренд/Модель). Это сильно ограничивает точность, так как модель не различает, условно, *BMW* и *Lada* при прочих равных характеристиках.
*   **План на v2.0:**
    *   Вернуть марку авто, используя **Target Encoding** или выделение топ-20 брендов.
    *   Предобработать признак `torque` и посмотреть как он влияет на качество.

    *   Добавить валидацию вводимых данных при загрузке CSV (как в режиме одиночного ввода).
